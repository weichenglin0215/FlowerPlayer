<Window x:ConnectionId='1'
    x:Class="FlowerPlayer.PlaylistWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:storage="using:Windows.Storage"
    xmlns:models="using:FlowerPlayer.Models"
    mc:Ignorable="d"
    Title="Playlist">

    <Grid x:ConnectionId='2' x:Name="RootGrid" AllowDrop="True"                                          >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Menu -->
            <RowDefinition Height="*"/>    <!-- Playlist -->
        </Grid.RowDefinitions>

        <MenuBar Grid.Row="0">
            <MenuBarItem Title="File">
                <MenuFlyoutItem x:ConnectionId='5' Text="開啟檔案..."                       />
                <MenuFlyoutItem x:ConnectionId='6' Text="Save Range As..."                        />
            </MenuBarItem>
        </MenuBar>

        <ListView x:ConnectionId='3' Grid.Row="1"                                                             Header="Playlist">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>


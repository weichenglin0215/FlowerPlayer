<Window x:ConnectionId='1'
    x:Class="FlowerPlayer.PlaylistWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Playlist">

    <Grid x:ConnectionId='2' AllowDrop="True"                                          >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Menu -->
            <RowDefinition Height="Auto"/> <!-- Splitter -->
            <RowDefinition Height="*"/>    <!-- Playlist -->
            <RowDefinition Height="Auto"/> <!-- Splitter -->
            <RowDefinition Height="150"/>  <!-- History -->
        </Grid.RowDefinitions>

        <MenuBar Grid.Row="0">
            <MenuBarItem Title="File">
                <MenuFlyoutItem x:ConnectionId='8' Text="開啟檔案..."                       />
                <MenuFlyoutItem x:ConnectionId='9' Text="Save Range As..."                        />
            </MenuBarItem>
            <MenuBarItem Title="Settings">
                <MenuFlyoutItem x:ConnectionId='7' Text="Open Settings..."                           />
            </MenuBarItem>
        </MenuBar>

        <Border Grid.Row="1" Height="2" Background="Gray"/>

        <ListView x:ConnectionId='3' Grid.Row="2"                                                Header="Playlist">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Border Grid.Row="3" Height="2" Background="Gray"/>

        <ListView x:ConnectionId='4' Grid.Row="4"                                               Header="History">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" Foreground="Gray"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>

